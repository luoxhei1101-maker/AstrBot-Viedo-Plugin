# 本插件不依赖 AstrBot 之外的任何第三方库。
#
# 唯一用到的 aiohttp 是 AstrBot 本体的核心依赖，插件里直接 import 即可，
# 不需要在这里重复声明（声明了反而可能在装插件时触发一次多余的 pip 检查）。
#
# 如果后续要加平台解析，需要引入新库时，按 AstrBot 规范写在这里，
# 比如：
#   httpx>=0.27
#   pycryptodome>=3.20
#
# 注意：AstrBot 官方规范要求插件不要使用 requests，请用 aiohttp / httpx 这类
# 异步网络库，否则会阻塞事件循环。
